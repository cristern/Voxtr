<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>Vǫxtr Athlete</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIconName</key>
	<string>AppIcon</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- Athlete Connection Foundation B1 (corrected — ParentApp CloudKit
	     sharing runtime fix): AthleteApp is the CKShare ACCEPTING app in
	     the Parent-invites/Athlete-accepts flow, so it needs this key to
	     handle incoming share URLs. Apple's own CKShare documentation
	     states this key is required by any app that implements CloudKit
	     Sharing at all — including the app that creates/presents a share
	     via UICloudSharingController — so ParentApp ALSO declares it (see
	     App/ParentApp/Info.plist's own comment); this is not an
	     accepting-app-only requirement. -->
	<key>CKSharingSupported</key>
	<true/>
	<!-- Athlete Connection QR-first V1: AthleteApp's own "Scan connection
	     code" screen requests camera access only when that screen
	     actually appears (never eagerly at launch) — this description is
	     required by iOS the first time that request happens, or the
	     process is terminated instead of prompting. -->
	<key>NSCameraUsageDescription</key>
	<string>Vǫxtr Athlete needs the camera to scan a connection code from a parent's device.</string>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
